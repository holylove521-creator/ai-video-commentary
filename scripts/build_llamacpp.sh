#!/bin/bash
# 本项目假设 llama.cpp 已在本地编译就绪，无需重新编译。
# 请直接编辑 config/model_config.yaml 中的 llamacpp.server_bin 字段，
# 指向你本地 llama-server 可执行文件的实际路径。
echo "请编辑 config/model_config.yaml 配置 llama-server 路径，无需重新编译。"
